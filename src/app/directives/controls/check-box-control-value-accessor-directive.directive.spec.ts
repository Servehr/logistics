import { CheckBoxControlValueAccessorDirectiveDirective } from './check-box-control-value-accessor-directive.directive';

describe('CheckBoxControlValueAccessorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckBoxControlValueAccessorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
