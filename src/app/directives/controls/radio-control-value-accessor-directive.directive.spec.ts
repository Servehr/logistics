import { inject, Injector } from "@angular/core";
import { RadioControlValueAccessorDirective } from "./radio-control-value-accessor-directive";


describe('RadioControlValueAccessorDirective=', () => {
  it('should create an instance', () => {
    const directive = new RadioControlValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
