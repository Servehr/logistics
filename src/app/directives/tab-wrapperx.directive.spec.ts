import { TabWrapperxDirective } from './tab-wrapperx.directive';

describe('TabWrapperxDirective', () => {
  it('should create an instance', () => {
    const directive = new TabWrapperxDirective();
    expect(directive).toBeTruthy();
  });
});
